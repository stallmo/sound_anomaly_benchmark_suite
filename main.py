"""
main.py — superseded by dcase_ae_baseline_runner.py.

Run the new CLI runner instead::

    uv run python dcase_ae_baseline_runner.py --help
"""

if __name__ == "__main__":
    raise SystemExit(
        "main.py has been replaced.\n"
        "Use:  uv run python dcase_ae_baseline_runner.py --help"
    )
